namespace Zork.Common
{
    enum Commands
    {
        QUIT,
        Q = QUIT,
        EXIT = QUIT,
        LOOK,
        L = LOOK,
        NORTH,
        N = NORTH,
        SOUTH,
        S = SOUTH,
        EAST,
        E = EAST,
        WEST,
        W = WEST,
        UNKNOWN,
        SCORE,
        REWARD,
        TAKE,
        DROP,
        INVENTORY,
        I = INVENTORY
    }
}